#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 *
 * @a: a positive or negative number
 *
 * Return: a positive number  or zero in all cases
 */
int _abs(int a)
{
	if (a <= 0)
	{
		a = -a;
		return (a);
	}
	else
	{
		return (a);
	}
}
